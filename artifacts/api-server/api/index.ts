import app from "../src/app.js";

// Vercel runs this Express app as a serverless function. Keep the local
// `app.listen()` bootstrap in src/index.ts for non-Vercel development only.
export default app;
